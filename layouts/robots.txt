{{- /*
  Allow-all. /private/ is suppressed by X-Robots-Tag: noindex, nofollow in
  static/_headers, NOT by a Disallow here. The two cannot coexist: a Disallow
  stops the crawler fetching the page, so it never sees the noindex header,
  and a URL discovered elsewhere can still be indexed. noindex is the stronger
  guarantee but only works on a crawlable route.

  What this template emits is NOT what the site serves. Cloudflare managed
  robots.txt is ON for this zone and PREPENDS its own block ahead of
  everything below: a Content-Signal line plus a `Disallow: /` group for nine
  training crawlers, none of which appear in this file. Editing here cannot
  remove them. Fetch https://hoiboy.uk/robots.txt before reasoning about what
  any crawler actually sees. cuarchitects.co.uk documents the same prepend.

  Training-crawler control over /private/ (rationale corrected 2026-07-20):
  this block used to say that dropping the Disallow left no training-crawler
  control over /private/, accepted because the operator did not mind training
  use "as long as our sites are cited for it", and because the citation
  condition was unmet with all six citation-class crawlers blocked at the
  edge. Every part of that is now stale. The operator reversed the position:
  training is blocked whether or not a citation follows, because no vendor
  offers a cite-for-training contract. The citation class is no longer
  blocked (the probe PASSes 6 of 6). And the prepended `Disallow: /` is
  site-wide, so /private/ is now covered against those nine crawlers.
  X-Robots-Tag noindex is still a SEARCH-INDEX directive doing no
  training-crawler work; what changed is that robots.txt now does it.
  Standing policy and zone settings: docs/research/16_AI_BOT_AND_SEO_POLICY.md

  No page links to /private/: the section index is headless, it is absent from
  the sitemap, and no content file references the path. A public post describes
  the tool in prose but does not link to it.

  It WAS however carried by the RSS feed, which linked it and duplicated its
  full rendered body into public/index.xml, a document advertised from every
  page via <link rel="alternate"> and matched by no X-Robots-Tag rule. The
  sitemap exclusion did not cover the feed. Fixed by `build.list: never` on the
  page itself (Ralph round 15); the feed dropped from 93 items to 92 and the
  page still renders for direct access.

  This sentence has now been WRONG TWICE in opposite directions: first claiming
  the page was already linked from a public post (it is not), then claiming it
  was reachable only by direct URL (the feed carried it). Do not restate it
  from memory. Re-run this, which greps the WHOLE built site rather than only
  href= in HTML, and so can actually fail:

      hugo --gc --minify -e production && grep -rl '/private/' public/ \
        | grep -v '^public/private/' \
        | grep -vx 'public/_headers' \
        | grep -v '^public/js/meet-recorder\..*\.schema\.json$'

  An empty result means nothing outside the section itself references it.
  Three matches are structural and are excluded above by name, each for a
  reason that cannot change: `public/_headers` IS the noindex rule, so it must
  name the path it suppresses, and the two `meet-recorder.*.schema.json` files
  carry a self-identifying absolute `$id` URL. Both are static passthroughs
  copied verbatim into every build. Do NOT widen these exclusions; anything
  else matching is a real finding.

  The two earlier versions of this instruction were both checks that could not
  have caught what they were written to catch. Round 14's checked only
  `href="/private` in HTML plus `content/` for the path, and returned zero
  while the RSS feed was carrying the page. Round 15's dropped the exclusions
  above, so it returned those three hits on every build and could never reach
  the clean state it defined, which is just as useless in the other direction.
  If you edit this command, seed a fake leak first (add the path to any
  content file, rebuild) and confirm it reports one.

  If /private/ ever holds something that must not enter a training corpus,
  this decision has to be revisited.
  See docs/research/16_AI_BOT_AND_SEO_POLICY.md and
  https://developers.google.com/search/docs/crawling-indexing/block-indexing
*/ -}}
User-agent: *
Disallow:

Sitemap: {{ "sitemap.xml" | absURL }}
