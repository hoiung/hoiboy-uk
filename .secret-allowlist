# Self-reference: secret detection script contains patterns as string literals
scripts/check-public-repo-secrets.py

# Import script contains source paths for legacy blog migration
scripts/import_aam_sql.py

# Research docs reference deployment tokens as examples (not real values)
docs/research/09_DEPLOYMENT.md

# Vendored SST3 mirror utility — contains canonical propagation patterns as
# string literals (same self-reference logic as check-public-repo-secrets.py).
# All other prior over-broad entries (research docs + helper scripts) reverted
# in favour of scrubbing the literal references at source.
scripts/sst3_mirror_utils.py

# Cloudflare Web Analytics beacon token — PUBLIC by design (ships in every
# page's rendered HTML; it is a site identifier, not a credential). Precise
# file:line so the rest of head.html is still secret-scanned.
# This entry is LINE-ANCHORED on purpose, so the rest of head.html keeps being
# scanned. The cost is that any insertion above the beacon moves it and the
# entry goes stale, which has now happened twice: line 101 after the branded
# default-card comment (#52), line 110 after the noindex meta block
# (blog-priv#55 Ralph round 15). If you edit head.html above this tag, re-run
# `python3 scripts/check-public-repo-secrets.py layouts/_partials/head.html`
# and update the number here. It fails loud, so it cannot drift silently.
layouts/_partials/head.html:110
